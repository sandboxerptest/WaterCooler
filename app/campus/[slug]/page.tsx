/**
 * A campus's own address: /campus/<organisation>.
 *
 * The same page as a room; the game boots straight onto the yard, so a
 * reload or a shared link lands there rather than in the lobby it was
 * reached from.
 */
export { default } from "../../page";
