/**
 * A floor of a building: /r/<slug>/floor/<level>.
 *
 * Floor 1 is the people's, Floor 2 the agents', both reached by the lift.
 * The same office page, keyed by the whole path; see lib/world/floors.ts.
 */
export { default } from "../../../../page";
