/**
 * A room's own URL: /r/<slug>.
 *
 * The same office, keyed by the slug in the path — which is also the way in,
 * so the link should be treated as a password.
 */
export { default } from "../../page";
